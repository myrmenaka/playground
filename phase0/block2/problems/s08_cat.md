# S08: Cat クラス

S06 で作った `Animal` クラスを継承して、`Cat` クラスを定義してください。

## 制約

- クラス名: `Cat`
- `Animal` クラスを継承する(`extends Animal`)
- `speak()` メソッドをオーバーライドして、「(名前): ニャー」と返すように変更する
- `Animal` クラスは S06 と同じものを再利用してOK(同じファイル内に再定義する)
- `declare(strict_types=1);` を先頭に書くこと

## 動作確認(`main` 部分)

クラス定義の下に、以下を行うコードを書いてください。

1. `Cat` を1つ作る(例: 名前 `"タマ"`)
2. `speak()` の戻り値を `echo` で表示

## 出力例

```
タマ: ニャー
```

## ファイル名

`my_answers/s08_cat.php`

## ヒント

<details>
<summary>クリックして表示</summary>

S07 で書いた `Dog` クラスとほぼ同じ構造になります。
鳴き声と、もし変えたければクラス名だけ変えればOKです。

</details>

---

書けたら「終わりました」と添えてコードを見せてください。レビューします。
詰まったら「ヒントください」または「答え見せて」と言ってください。